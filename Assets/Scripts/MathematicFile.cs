using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MathematicFile : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
      for(int i = 5;i <= 5; i++){
    for(int j = 1;j <= 10; j++){
            Debug.Log(i + "*" + j + "=" + (i*j));
        
      }

      }
    }

 
}
